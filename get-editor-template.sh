REPO_DIR=[[REPO_DIR]]
CURRENT_DIR=$(pwd)

cp $REPO_DIR/che-plugin-registry/che-plugin-template.yaml $CURRENT_DIR/che-plugin.yaml
cp $REPO_DIR/che-plugin-registry/meta-template.yaml $CURRENT_DIR/meta.yaml
